const Obj = {
    js : "Javascript",
    cpp : "C++",
    py : "Python"
}

for (const key in Obj) {

    // console.log(`${key} shortcut is for ${Obj[key]}`);
}

const prog = ["Python", "CPP", "Javascript"]

for (const key in prog) {
    // console.log(key);
}
